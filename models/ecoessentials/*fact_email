{{ config(
    materialized = 'table',
    database = 'group3project',
    schema = 'ecoessentials_dw_source'
    )
}}

SELECT
e.emailid,
e.campaignid,
product_id,
e.customerid,
time_id,
e.subscriberid,
e.emaileventid
FROM {{ source('ecoessentials_landing', 'salesforce_marketing_emails') }} e
